<?php
/**
 * Настройки защиты ключей E2EE (этап 4).
 * Скопируйте в e2ee_key_backup.php и при необходимости измените.
 * Редактируется из админки: раздел «Защита ключей».
 */
return [
    // Запросов blob в час на пользователя (rate limit)
    'rate_limit_per_hour' => 10,
    // Неудачных попыток расшифровки до временной блокировки выдачи blob
    'failures_before_lockout' => 5,
    // Длительность блокировки (часы)
    'lockout_hours' => 24,
    // Задержка на клиенте после неверного пароля: базовая (сек)
    'client_delay_base_sec' => 2,
    // Задержка на клиенте: максимальная (сек)
    'client_delay_max_sec' => 300,
    // Итерации PBKDF2 для вывода ключа из пароля (опционально, по умолчанию 100000)
    'kdf_iterations' => 100000,
];
